[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ternrefine"
version = "0.1.0"
description = "Gradient-guided fixed-capacity refinement utilities for ternary LLMs"
readme = "README.md"
requires-python = ">=3.10"
authors = [{name = "Anonymous"}]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

