# Validated on the BitaHub CEGSP container with Python 3.11.15.
# Install PyTorch separately from the CUDA 12.4 wheel index first:
#   python -m pip install --index-url https://download.pytorch.org/whl/cu124 torch==2.5.1
# Then install this file from the regular PyPI index.
transformers==4.46.3
datasets==3.0.1
accelerate==1.0.1
numpy==2.4.6
pandas==2.2.2
scipy==1.17.1
tokenizers==0.20.3
safetensors==0.4.5
huggingface-hub==0.36.2
pyarrow>=17.0.0
tqdm>=4.66.0
requests>=2.32.0
